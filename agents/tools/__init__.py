"""
Portable tool functions — no cloud dependency.

Each module exposes pure functions that take explicit parameters and return
data.  Used directly in local/cloud-agnostic K8s, wrapped as Lambda / Azure
Function / Cloud Function handlers in cloud-native deployments.
"""
